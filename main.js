const arrangeSum = (i) => {
	let inputList = i.split(" ");
	let mapValue = new Map();

	// for (let i=0; i<= inputList.length; i++){
	// 	let numList = inputList[i].slice();
	// 	let sum = 0;
	// 	for (let j=0; j<numList.length;j++) {
	// 		sum += Number(numList[j]);
	// 	}
	// 	mapValue.inputList[i] = sum;
	// }

  return inputList;	
}

console.log(arrangeSum("103 4444 99 123 2000 100 10"));
